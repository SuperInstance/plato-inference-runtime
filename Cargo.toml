[package]
name = "plato-inference-runtime"
version = "0.1.0"
edition = "2021"
description = "Inference runtime — model lifecycle, batch scheduling, resource management, adapter loading"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
